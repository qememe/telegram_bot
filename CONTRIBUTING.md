# Руководство для разработчиков

Спасибо за интерес к проекту! Это руководство поможет вам начать работу над проектом.

## Как внести вклад

### 1. Форк репозитория

1. Перейдите на страницу репозитория на GitHub
2. Нажмите кнопку "Fork" в правом верхнем углу
3. Это создаст копию репозитория в вашем аккаунте

### 2. Клонирование форка

```bash
git clone https://github.com/YOUR_USERNAME/telegram_bot.git
cd telegram_bot
```

### 3. Создание feature branch

Создайте новую ветку для вашей функции или исправления:

```bash
git checkout -b feature/your-feature-name
# или для исправления бага:
git checkout -b fix/your-bug-fix
```

**Соглашения по именованию веток:**
- `feature/` - для новых функций
- `fix/` - для исправления багов
- `docs/` - для изменений в документации
- `refactor/` - для рефакторинга кода

### 4. Локальная настройка

```bash
# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Создайте .env файл
cp .env.example .env
# Отредактируйте .env и добавьте свои ключи
```

### 5. Запуск локально

```bash
python main.py
```

### 6. Внесение изменений

- Пишите чистый, читаемый код
- Следуйте стилю кода проекта (PEP 8 для Python)
- Добавляйте комментарии к сложным участкам кода
- Обновляйте документацию при необходимости

### 7. Коммиты

Используйте понятные сообщения коммитов:

**Формат:**
```
<тип>: <краткое описание>

<подробное описание (опционально)>
```

**Типы коммитов:**
- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование кода (не влияет на функциональность)
- `refactor:` - рефакторинг кода
- `test:` - добавление или изменение тестов
- `chore:` - обновление зависимостей, конфигурации и т.д.

**Примеры:**
```bash
git commit -m "feat: добавлена поддержка обработки видео"
git commit -m "fix: исправлена ошибка при обработке больших изображений"
git commit -m "docs: обновлена документация по установке"
```

### 8. Push изменений

```bash
git push origin feature/your-feature-name
```

### 9. Создание Pull Request

1. Перейдите на страницу вашего форка на GitHub
2. Нажмите "New Pull Request"
3. Выберите базовую ветку (обычно `main`) и вашу feature ветку
4. Заполните описание PR:
   - Что было изменено
   - Почему это было изменено
   - Как это было протестировано
5. Нажмите "Create Pull Request"

### 10. Code Review

После создания PR ваши изменения будут проверены. Возможно, потребуется внести дополнительные изменения по замечаниям ревьюеров.

## Стандарты кода

### Python

- Следуйте PEP 8
- Используйте type hints где возможно
- Документируйте функции и классы с помощью docstrings
- Максимальная длина строки: 100 символов

### Импорты

Используйте следующий порядок импортов:
1. Стандартная библиотека Python
2. Сторонние библиотеки
3. Локальные импорты

Пример:
```python
import asyncio
from datetime import datetime

import aiohttp
from aiogram import Bot

from config import TELEGRAM_TOKEN
from utils.logger import setup_logger
```

### Async функции

Все функции, работающие с I/O, должны быть асинхронными:
```python
async def process_message(message: str) -> str:
    """Обрабатывает сообщение."""
    # ...
```

## Тестирование

Перед отправкой PR убедитесь, что:
- Код запускается без ошибок
- Все существующие функции работают
- Новые функции протестированы вручную

## Вопросы?

Если у вас есть вопросы, создайте Issue в репозитории или свяжитесь с maintainer'ами проекта.

Спасибо за ваш вклад! 🎉

