# Telegram Support Bot

Telegram бот для автоматической поддержки клиентов. Обрабатывает текст, фото (OCR) и аудио (Whisper), отвечает через Claude API.

## Features

- 📝 **Обработка текстовых сообщений** - получайте ответы от AI (Claude через ProxyAPI)
- 📷 **OCR из изображений** - извлечение текста из фото через EasyOCR
- 🎤 **Транскрибирование аудио** - конвертация голосовых сообщений в текст через Whisper API
- 💾 **История диалогов** - сохранение всех сообщений и ответов в базе данных SQLite
- 🔄 **Retry логика** - автоматические повторы при ошибках API
- 📊 **Логирование** - подробные логи всех операций

## Requirements

- Python 3.9+
- Ubuntu/Linux (рекомендуется)
- Tesseract OCR (для OCR обработки)
- FFmpeg (для конвертации аудио)

## Installation

### 1. Клонирование репозитория

```bash
git clone https://github.com/YOUR_USERNAME/telegram_bot.git
cd telegram_bot
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Установка системных зависимостей

```bash
# Для Ubuntu/Debian
sudo apt-get update
sudo apt-get install -y tesseract-ocr tesseract-ocr-rus tesseract-ocr-eng ffmpeg
```

### 5. Настройка переменных окружения

```bash
cp .env.example .env
```

Отредактируйте файл `.env` и укажите свои ключи:
- `TELEGRAM_TOKEN` - токен бота от @BotFather
- `PROXYAPI_KEY` - API ключ от proxyapi.ru

### 6. Запуск бота

```bash
python main.py
```

## Configuration

### Получение TELEGRAM_TOKEN

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в файл `.env`

### Получение PROXYAPI_KEY

1. Зарегистрируйтесь на [proxyapi.ru](https://proxyapi.ru)
2. Перейдите в раздел API Keys
3. Создайте новый ключ или используйте существующий
4. Скопируйте ключ в файл `.env`

### Переменные окружения

| Переменная | Описание | Обязательная | По умолчанию |
|------------|----------|--------------|--------------|
| `TELEGRAM_TOKEN` | Токен Telegram бота | Да | - |
| `PROXYAPI_KEY` | API ключ ProxyAPI | Да | - |
| `PROXYAPI_URL` | URL ProxyAPI | Нет | `https://api.proxyapi.ru/openai/v1` |
| `DATABASE_URL` | URL базы данных | Нет | `sqlite:///bot_data.db` |
| `LOG_LEVEL` | Уровень логирования | Нет | `INFO` |

## Database

Бот использует SQLite базу данных для хранения истории диалогов. База данных создается автоматически при первом запуске.

### Структура базы данных

- **users** - информация о пользователях Telegram
- **messages** - все сообщения от пользователей (текст, фото, аудио)
- **responses** - ответы бота с информацией о модели и токенах

База данных инициализируется автоматически при запуске бота через функцию `init_db()` в `database/models.py`.

## Troubleshooting

### Бот не запускается

- Проверьте, что все переменные окружения установлены в файле `.env`
- Убедитесь, что токен Telegram бота корректный
- Проверьте логи на наличие ошибок

### OCR не работает

- Убедитесь, что Tesseract установлен: `tesseract --version`
- Проверьте, что установлены языковые пакеты: `tesseract-ocr-rus`, `tesseract-ocr-eng`
- При первом запуске EasyOCR загрузит модели (может занять время)

### Аудио не транскрибируется

- Проверьте, что FFmpeg установлен: `ffmpeg -version`
- Убедитесь, что API ключ ProxyAPI корректный и имеет доступ к Whisper API
- Проверьте размер аудио файла (максимум 20MB)

### Ошибки подключения к API

- Проверьте интернет-соединение
- Убедитесь, что API ключ ProxyAPI активен и имеет достаточный баланс
- Проверьте логи на наличие ошибок 429 (Too Many Requests) или 500 (Server Error)

## Структура проекта

```
telegram_bot/
├── main.py                    # Точка входа
├── config.py                  # Конфигурация и переменные окружения
├── requirements.txt           # Python зависимости
├── .env.example              # Шаблон переменных окружения
├── .gitignore                # Игнорируемые файлы Git
├── README.md                 # Документация
├── Dockerfile                # Конфигурация Docker
├── docker-compose.yml        # Docker Compose конфигурация
├── handlers/                 # Обработчики сообщений
│   ├── __init__.py
│   ├── message_handler.py    # Текстовые сообщения
│   ├── photo_handler.py      # Фото
│   └── audio_handler.py      # Аудио
├── services/                 # Сервисы обработки
│   ├── __init__.py
│   ├── api_service.py        # Работа с ProxyAPI
│   ├── ocr_service.py        # OCR обработка
│   └── transcription_service.py  # Транскрибирование
├── database/                 # База данных
│   ├── __init__.py
│   ├── models.py             # Модели БД
│   └── db.py                 # Подключение
└── utils/                    # Утилиты
    ├── __init__.py
    └── logger.py             # Логирование
```

## Использование

1. Запустите бота командой `python main.py`
2. Откройте Telegram и найдите вашего бота
3. Отправьте команду `/start` для начала работы
4. Отправьте:
   - **Текстовое сообщение** - получите ответ от AI
   - **Фото с текстом** - получите извлеченный текст и ответ от AI
   - **Аудио/голосовое сообщение** - получите транскрипцию и ответ от AI

## License

MIT License
